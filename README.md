# Test Wright
Test Wright is a tool for integrating with existing development workflows.

## Usage
1. Trigger a test run using the `trigger_test` method.
2. Store test results in a repository or database using the `store_test_results` method.
3. Retrieve integration documentation using the `get_integration_docs` method.

## Testing
Run tests using `python -m pytest`.
